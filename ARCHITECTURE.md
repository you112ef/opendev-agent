# معمارية OpenDevAgent Platform

## 🎯 نظرة عامة على المعمارية

OpenDevAgent Platform مستوحاة من معمارية **Kilo Code** وتستخدم نمط **Plan-Act-Observe-Fix** مع نظام وكلاء متعددة متخصصة.

## 📦 طبقات المنصة

### 1. الطبقة الأمامية (Frontend Layer)

**الموقع**: `/frontend`
**المكدس**: Next.js, React 18, TypeScript, Tailwind CSS

```
frontend/
├── pages/              # صفحات Next.js
│   ├── index.tsx      # الصفحة الرئيسية
│   └── _app.tsx       # تطبيق App
├── components/        # مكونات React
│   ├── ApiKeyInputForm.tsx
│   ├── TaskCreationWizard.tsx
│   ├── AgentStatusDashboard.tsx
│   ├── RealTimeLogViewer.tsx
│   └── NotificationSystem.tsx
├── lib/
│   ├── store.ts       # Zustand Store
│   └── api.ts         # HTTP Client
└── styles/
    └── globals.css    # أنماط عام
```

**المسؤوليات**:
- إدارة واجهة المستخدم
- معالجة المدخلات
- عرض حالة النظام
- إدارة جلسات المستخدم
- التواصل مع API

### 2. الطبقة الخلفية (Backend Layer)

**الموقع**: `/backend`
**المكدس**: FastAPI, Python 3.11+

```
backend/
├── main.py            # تطبيق FastAPI
├── requirements.txt   # المكتبات
└── agent_logic/
    ├── orchestrator.py    # منسق الوكلاء
    ├── models.py          # نماذج البيانات
    └── tools/
        └── sandbox_executor.py
```

**المسؤوليات**:
- إدارة API Endpoints
- تنسيق الوكلاء
- معالجة المهام
- الاتصال بـ OpenRouter API
- إدارة الحالة

### 3. طبقة الذكاء الاصطناعي (AI Layer)

**الموقع**: `/backend/agent_logic`

#### Agents الثلاثة:

```
┌──────────────────────────────────────────┐
│         Agent Orchestrator                │
│  (منسق الوكلاء - يدير التدفق الكامل)   │
└────────┬─────────────────────────────────┘
         │
         ├──────────────────┬──────────────────┬──────────────────┐
         │                  │                  │                  │
    ┌────▼────────┐   ┌────▼────────┐   ┌────▼────────┐   
    │ Architect   │   │ Coder       │   │ Debugger    │   
    │ Agent       │   │ Agent       │   │ Agent       │   
    ├─────────────┤   ├─────────────┤   ├─────────────┤   
    │ GPT-4o      │   │ Codestral   │   │ Claude 3.5  │   
    └─────────────┘   └─────────────┘   └─────────────┘   
```

**Architect Agent**:
- النموذج: `openai/gpt-4o`
- المسؤولية: التخطيط المعماري
- الخرجات: خطة معمارية شاملة

**Coder Agent**:
- النموذج: `mistral/codestral-22b`
- المسؤولية: توليد الكود
- الخرجات: كود إنتاجي جاهز

**Debugger Agent**:
- النموذج: `anthropic/claude-3-5-sonnet`
- المسؤولية: التحليل والتصحيح
- الخرجات: تقرير التحليل والتوصيات

### 4. طبقة التنفيذ الآمنة (Sandbox Layer)

**الموقع**: `/sandbox`
**التكنولوجيا**: Docker Containers

```
┌─────────────────────────────────────────┐
│  Sandbox Executor                       │
│  (منفذ الرمل - بيئة معزولة آمنة)      │
└────────┬────────────────────────────────┘
         │
         ├─ Resource Limits
         │  ├─ CPU: 1 Core
         │  ├─ Memory: 512MB
         │  └─ Timeout: 300s
         │
         ├─ Isolation
         │  ├─ Network Disabled
         │  ├─ File System Sandbox
         │  └─ Process Isolation
         │
         └─ Language Support
            ├─ Python
            ├─ JavaScript/TypeScript
            ├─ Java
            └─ More...
```

## 🔄 دورة العمل: Plan-Act-Observe-Fix

### المرحلة 1: PLAN (التخطيط)

```
┌─────────────────────────────────────┐
│ Input: Task Description             │
│ "إنشاء تطبيق إدارة المهام"         │
└────────────┬────────────────────────┘
             │
             ▼
        ┌─────────────────────┐
        │ Architect Agent     │
        │ (GPT-4o)           │
        └────────┬────────────┘
                 │
                 ▼
        ┌─────────────────────┐
        │ Architecture Plan:  │
        │ - System Design     │
        │ - Components        │
        │ - Data Flow         │
        │ - Technology Stack  │
        └─────────────────────┘
```

### المرحلة 2: ACT (التنفيذ)

```
        ┌──────────────────────┐
        │ Architecture Plan    │ (من المرحلة 1)
        └──────────┬───────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │ Coder Agent          │
        │ (Codestral-22b)      │
        └──────────┬───────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │ Generated Code:      │
        │ - Main Application   │
        │ - Configuration      │
        │ - Utilities          │
        │ - Error Handling     │
        └──────────────────────┘
```

### المرحلة 3: OBSERVE (الملاحظة)

```
        ┌──────────────────────┐
        │ Generated Code       │
        └──────────┬───────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │ Sandbox Executor     │
        │ (Docker Container)   │
        └──────────┬───────────┘
                   │
                   ├─ Execute Code
                   ├─ Collect Output
                   ├─ Capture Errors
                   └─ Monitor Resources
                   │
                   ▼
        ┌──────────────────────┐
        │ Execution Results:   │
        │ - Output             │
        │ - Errors             │
        │ - Performance Metrics│
        └──────────────────────┘
```

### المرحلة 4: FIX (الإصلاح)

```
        ┌──────────────────────┐
        │ Execution Results    │ (من المرحلة 3)
        └──────────┬───────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │ Debugger Agent       │
        │ (Claude 3.5 Sonnet) │
        └──────────┬───────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │ Analysis & Feedback: │
        │ - Issues Found       │
        │ - Improvements       │
        │ - Security Review    │
        │ - Best Practices     │
        └──────────────────────┘
```

## 🔌 نقاط التكامل

### OpenRouter API

```
┌─────────────────────────────────┐
│ OpenRouter API Integration      │
├─────────────────────────────────┤
│ Base URL:                       │
│ https://openrouter.ai/api/v1    │
├─────────────────────────────────┤
│ Models:                         │
│ - openai/gpt-4o (Architect)    │
│ - mistral/codestral-22b (Coder)│
│ - anthropic/claude-3-5-sonnet  │
│   (Debugger)                   │
│ - meta-llama/llama-3.1-70b     │
│   (Fallback)                   │
├─────────────────────────────────┤
│ Authentication: Bearer Token    │
└─────────────────────────────────┘
```

### Redis Cache

```
┌─────────────────────────────────┐
│ Redis Cache Layer               │
├─────────────────────────────────┤
│ - Task State Caching            │
│ - Session Management            │
│ - Rate Limiting                 │
│ - Real-time Updates             │
└─────────────────────────────────┘
```

## 📊 تدفق البيانات

```
User Input
    │
    ▼
┌──────────────────┐
│ Frontend         │
│ (Next.js/React)  │
└────────┬─────────┘
         │
         ▼
    ┌────────────────────┐
    │ API Request        │
    │ + API Key Validation│
    └────────┬───────────┘
             │
             ▼
    ┌────────────────────┐
    │ FastAPI Backend    │
    │ - Process Input    │
    │ - Create Task      │
    │ - Start Orchestrator
    └────────┬───────────┘
             │
             ▼
    ┌────────────────────┐
    │ Agent Orchestrator │
    │ - Plan (Architect) │
    │ - Act (Coder)      │
    │ - Observe (Sandbox)│
    │ - Fix (Debugger)   │
    └────────┬───────────┘
             │
             ▼
    ┌────────────────────┐
    │ Real-time Updates  │
    │ (Polling)          │
    └────────┬───────────┘
             │
             ▼
    ┌────────────────────┐
    │ Display Results    │
    │ in Dashboard       │
    └────────────────────┘
```

## 🔐 معمارية الأمان

```
┌─────────────────────────────────┐
│ Security Architecture           │
├─────────────────────────────────┤
│                                 │
│ 1. Input Layer                  │
│    - HTTPS Only                 │
│    - API Key Validation         │
│    - Rate Limiting              │
│                                 │
│ 2. Processing Layer             │
│    - Memory-only Key Storage    │
│    - No Persistent Secrets      │
│    - Encrypted Channels         │
│                                 │
│ 3. Execution Layer              │
│    - Docker Container Isolation │
│    - Resource Limits            │
│    - Network Disabled           │
│    - Process Sandboxing         │
│                                 │
│ 4. Monitoring Layer             │
│    - Audit Logging              │
│    - Resource Tracking          │
│    - Error Handling             │
│                                 │
└─────────────────────────────────┘
```

## 📈 قابلية التوسع

### الأفقية (Horizontal)
- تشغيل عدة instances من Frontend
- توزيع Backend عبر Kubernetes
- استخدام Redis Cluster

### العمودية (Vertical)
- زيادة موارد الحاوية
- تحسين استعلامات قاعدة البيانات
- تحسين استخدام الذاكرة

## 📝 المراقبة والتسجيل

```
┌─────────────────────────────────┐
│ Monitoring & Logging            │
├─────────────────────────────────┤
│                                 │
│ Frontend:                       │
│ - User Actions Tracking         │
│ - Error Boundaries              │
│ - Performance Metrics           │
│                                 │
│ Backend:                        │
│ - Request Logging               │
│ - Task Execution Traces         │
│ - API Call Monitoring           │
│ - Error Tracking                │
│                                 │
│ Sandbox:                        │
│ - Code Execution Output         │
│ - Resource Usage                │
│ - Exit Codes                    │
│ - Timeout Tracking              │
│                                 │
└─────────────────────────────────┘
```

---

**آخر تحديث**: November 2024
